#include "main.h"
/**
 * _isdigit - checks if numbers are 0 to 9
 * @c: the character to check
 *
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
