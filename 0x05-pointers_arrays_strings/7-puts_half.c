/**
 * puts2 - prints second half of a given string. 
 * starting with the \  first character, followed by a new line.
 * @str: string to be printed.
 * Return: 0
 */
void puts_half(char *str)
{
	 int count = 0;

while (*str)
{
	count++;
	str++;
}
}
