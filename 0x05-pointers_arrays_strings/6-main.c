#include "main.h"
/**
 * main - check the code
 * puts2 - writes a string to the output
 * Return: Always 0.
 */
int main(void)
{
	char *str;
	char puts2();

	str = "0123456789";
	puts2(str);
	return (0);
}
