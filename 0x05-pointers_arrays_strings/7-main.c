#include "main.h"
/**
 * main - check the code
 * puts_half(str) - print the second half of the string
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "0123456789";
	puts_half(str);
	return (0);
}
